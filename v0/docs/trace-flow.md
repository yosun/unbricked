# Trace Flow (Provenance + Derivation)

Goal: any artifact can be traced backward/forward:
- From a visible segment → source image region → operator run → upstream spaces.
- GraphPatch and OperatorRun form the “audit spine”.
