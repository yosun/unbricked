# Unbricked — Agent Instructions (always-on)

Follow **CLAUDE.md**. It is the canonical workflow + standards.

Hard rules:
- Do not change production code until `notes/plan.md` exists AND is approved.
- Use `notes/research.md` for findings; use `notes/plan.md` for the implementation plan + TODOs.
- Implement only when told: **"implement it all"**.

MVP pillars:
- Everything is a Space you can enter.
- 3D-first UI; 2D is a projection.
- Layers are spatial slices (prism).
- Segmentation-first vs holistic reconstruction are both first-class paths.
